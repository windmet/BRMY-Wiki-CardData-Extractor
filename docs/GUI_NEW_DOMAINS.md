# 新档案 GUI 接入（2026-09-09）

最新冻结进度见 [候选验收](FROZEN_CANDIDATE_20260909.md)：04b4397 的EXE已通过真实离线五任务及坐标验证，可见GUI仍待验收。下文逐批记录中的“未重建”仅指对应批次当时状态。

源码 GUI 将任务分为卡牌与收藏、活动、语音、生日、剧情与其他五组，保留跨组选中状态。分组下有常驻说明，明确生日周期台词、年度生日档案与ACB生日祝福的区别。新增 OJT、年度生日、剧情目录、收藏、双人主页五项。

主数据域复用正式masterdata；双人主页复用21角色包家族资源选择，同时要求masterdata与ACB。本地模式在生成前校验两者路径。

正式缓存端到端离线生成五任务均PASS，整体PASS_WITH_WARNINGS（原名称缺项保留），5工作簿、13登记产物。正式ACB双人210对、0缺项。证据：`%TEMP%/brmy-new-domains-offline-w9sea3sk/audit_output/output_receipt.json`。140项测试及仓库验证通过。

本批仅完成源码接入、隐藏组件测试及后台真实生成；不代表可见布局、键盘流程或新EXE验收已完成。新分组仍须可见检查，再进行干净构建与冻结版本实际点击验收。

## 资源预览与任务说明

主界面直接说明在线预览可能下载基础数据 masterdata；按钮按模式显示“预览资源与缓存”或“检查本地输入”。预览在基础数据准备后，对计划中每个资源复用 Provider 离线校验（清单指纹、大小、SHA-256），记录 `cache_assessment`。缺失/损坏项不在预览中补下载；只有显式校验结果才展示数量，旧响应没有该字段时不猜缓存数。

正式缓存五新域预览共85资源，85已校验、0未通过。证据：`%TEMP%/brmy-preview-cache-416ogvwu/audit_output/resource_plan.json`。这是本地缓存快照结果，不代表线上最新版本；masterdata 也计入计划总数。

新增“任务说明”按钮，逐任务说明覆盖19项，支持键盘聚焦按钮及 Esc 关闭说明窗口。两个生日入口、ACB祝福、OJT档案与独立Chart用途明确区分。存在运行告警的文件显示“已生成，请核对”，失败产物仍显示“不完整”；receipt 保留原信息。业务告警与资源提示的进一步分层仍待完成。

148项测试及仓库验证通过，包括缓存损坏后预览不重下载、缺校验结果不猜数量、帮助覆盖与关闭绑定。未执行可见 GUI/实际键盘验收，本批未重建EXE。

## 运行反馈分层

Resolver 对离线快照、未使用的清单类别提供明确 kind，保留原提示文本。GUI 只将这些资源提示及首次建立 schema 基线放入“资源与运行提示”；业务告警、未知 kind 与旧字符串告警保守地放入“内容与数据需要核对”。运行失败独立展示。不根据文字关键词降级，不修改 receipt 的 PASS_WITH_WARNINGS 状态。

文件行按业务告警的 domain 显示“已生成，请核对”；无 domain 或资源缺项告警按整次运行处理，失败产物仍显示“不完整”。原始结果完整保留在详情及 receipt。

正式离线五任务真实生成均 PASS，13产物；整体 PASS_WITH_WARNINGS。分层为2项业务核对、3项资源/基线提示，0错误。证据：`%TEMP%/brmy-feedback-real-xxka9tnb/feedback_verification.json` 及同目录 `audit_output/output_receipt.json`。151项测试与仓库验证通过。这里验证的是后台生成与组件行为，可见界面和冻结版本仍待验收。
